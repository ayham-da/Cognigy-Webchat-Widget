import { SocketClient } from '@cognigy/socket-client';
import { Options } from '@cognigy/socket-client/lib/interfaces/options';
import { IWebchatConfig } from './interfaces/webchat-config';
export { Options };
export declare class WebchatClient extends SocketClient {
    webchatConfig: IWebchatConfig;
    private analytics;
    private static createDefaultWebchatOptions;
    private static createWebchatOptions;
    private static fetchWebchatConfig;
    private static getEndpointBaseUrl;
    private static getEndpointUrlToken;
    constructor(webchatConfigUrl: string, options?: Partial<Options>);
    readonly webchatConfigUrl: string;
    loadWebchatConfig: () => Promise<void>;
    connect(): Promise<any>;
    emitAnalytics(type: string, payload?: any): void;
    registerAnalyticsService(handler: (event: {
        type: string;
        payload?: any;
    }) => void): void;
}
